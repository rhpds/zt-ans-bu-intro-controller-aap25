---
# solve.yml -- Module 04 (Creating your first Project)
# Creates: Apache playbooks project (Git SCM), waits for sync to complete.

- name: Log solve start
  ansible.builtin.shell: echo "Solving module-04" >> /tmp/progress.log

- name: Get Default organization ID
  ansible.builtin.uri:
    url: "{{ aap_host }}/api/controller/v2/organizations/?name=Default"
    user: "{{ aap_username }}"
    password: "{{ aap_password }}"
    force_basic_auth: true
    validate_certs: false
  register: r_org

- name: Check if Apache playbooks project exists
  ansible.builtin.uri:
    url: "{{ aap_host }}/api/controller/v2/projects/?name=Apache+playbooks"
    user: "{{ aap_username }}"
    password: "{{ aap_password }}"
    force_basic_auth: true
    validate_certs: false
  register: r_project_check

- name: Create Apache playbooks project
  ansible.builtin.uri:
    url: "{{ aap_host }}/api/controller/v2/projects/"
    method: POST
    user: "{{ aap_username }}"
    password: "{{ aap_password }}"
    force_basic_auth: true
    validate_certs: false
    body_format: json
    body:
      name: "Apache playbooks"
      organization: "{{ r_org.json.results[0].id | int }}"
      scm_type: "git"
      scm_url: "https://github.com/ansible-tmm/instruqt-wyfp.git"
    status_code: [200, 201]
  when: r_project_check.json.count == 0

- name: Wait for project sync to complete
  ansible.builtin.uri:
    url: "{{ aap_host }}/api/controller/v2/projects/?name=Apache+playbooks"
    user: "{{ aap_username }}"
    password: "{{ aap_password }}"
    force_basic_auth: true
    validate_certs: false
  register: r_project_status
  until: r_project_status.json.results[0].status in ['successful', 'failed']
  retries: 30
  delay: 10
